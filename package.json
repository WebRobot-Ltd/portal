{
  "name": "webrobot-vitepress-site",
  "version": "1.0.0",
  "type": "module",
  "description": "WebRobot enterprise showcase website built with VitePress",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "build:widget": "vite build --config vite.widget.config.mjs",
    "preview": "vitepress preview",
    "docker:build": "docker build -t webrobot-vitepress-site .",
    "docker:run": "docker run -p 8080:80 webrobot-vitepress-site",
    "openapi:sync": "cp ../webrobot-etl-api-doc/openapi.json public/openapi.json && echo 'openapi.json synced from webrobot-etl-api-doc' || echo 'sibling repo webrobot-etl-api-doc not found — pulled openapi.json must be updated by CI'"
  },
  "keywords": [
    "webrobot",
    "etl",
    "spark",
    "ai",
    "documentation"
  ],
  "author": "WebRobot Team",
  "license": "MIT",
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
